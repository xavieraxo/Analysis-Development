@page "/"
@layout SimpleLayout
@inject NavigationManager Navigation
<PageTitle>Inicio - Multi-Agent System</PageTitle>

<div style="display: flex; flex-direction: column; justify-content: center; align-items: center; min-height: 60vh; padding: 2rem;">
    <!-- Frase superior temporal -->
    <MudText Typo="Typo.h6" Align="Align.Center" Class="mb-6" Style="font-style: italic; color: rgba(0,0,0,0.6);">
        todo lo bueno está por venir
    </MudText>
    
    <!-- Botón de Login centrado -->
     <MudButton Variant="Variant.Filled" 
              Color="Color.Primary" 
              Size="Size.Large"
               Href="/login"
              Style="min-width: 200px;">
        Iniciar Sesión
    </MudButton>  
     @*<MudButton Color="Color.Primary"
               Variant="Variant.Filled"
               OnClick="@(() => Navigation.NavigateTo("/login"))">
        Iniciar Sesión
    </MudButton>*@
</div>
